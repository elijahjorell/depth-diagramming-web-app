const activeSpace = new Space(state.space);
const activeScreen = new Screen(state.screen);
const activeCursor = new Cursor();
const activeKeyboard = new Keyboard(configuration.keyboard);
const activeMouse = new Mouse(configuration.mouse);

